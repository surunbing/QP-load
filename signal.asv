clc, clear
close all

N = 2000;
T = 1 / 80;
N_E = 4 / T;
x_r = zeros(N, 1);
y_r = zeros(N, 1);
z_r = zeros(N, 1);
for i = 1 : N
    if i < N_E
        omega = i / N_E * 0.4;
        x_r(i) = 2 * sin(ometa * 
end